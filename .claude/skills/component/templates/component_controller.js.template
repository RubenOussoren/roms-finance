import { Controller } from "@hotwired/stimulus"

// {{CONTROLLER_NAME}} Stimulus Controller
// Component-specific controller for {{CLASS_NAME}}
export default class extends Controller {
  static targets = []
  static values = {}

  connect() {
    // Initialize component
  }

  disconnect() {
    // Cleanup
  }
}
