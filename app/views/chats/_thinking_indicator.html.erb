<%# locals: (chat:, message: "Thinking ...") -%>

<div id="thinking-indicator" class="flex items-start gap-3" role="status" aria-live="assertive" aria-label="Assistant is thinking">
  <%= render "chats/ai_avatar" %>
  <p class="text-sm text-secondary animate-pulse"><%= message %></p>
</div>
